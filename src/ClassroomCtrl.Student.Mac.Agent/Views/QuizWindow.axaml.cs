using Avalonia.Controls;

namespace ClassroomCtrl.Student.Mac.Agent.Views;

/// <summary>Centered, top-most quiz/survey window. Content + submit behaviour live in the ViewModel.</summary>
public partial class QuizWindow : Window
{
    public QuizWindow() => InitializeComponent();
}
