<Window x:Class="ClassroomCtrl.Student.Agent.Setup.TeacherIPDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{DynamicResource Dlg_ClassroomSetup}"
        Height="320" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{DynamicResource Dlg_ClassroomSetup}" FontSize="22" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Text="{DynamicResource Dlg_EnterTeacherIP}"
                   FontSize="13" Foreground="Gray" Margin="0,4,0,16"/>

        <TextBox Grid.Row="2" x:Name="IPTextBox"
                 materialDesign:HintAssist.Hint="{DynamicResource Hint_IPExample}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 FontSize="16" Padding="8"/>

        <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="Red"
                   Margin="0,12,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="{DynamicResource Btn_Cancel}" Width="100" Margin="0,0,8,0" Click="Cancel_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="{DynamicResource Btn_Save}" Width="100" Click="Save_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>