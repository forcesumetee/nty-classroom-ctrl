<UserControl x:Class="ClassroomCtrl.Teacher.Controls.HelpIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="20" d:DesignHeight="20">
    <UserControl.Resources>
        <!-- Phase 3 Section B — small "?" affordance.  Appears next to section headers and
             important buttons to surface a Loc-keyed explanation popup on click.  Hover
             tints the circle with the accent so it reads as a real interactive element. -->
        <Style x:Key="HelpButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Text.Muted}"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{DynamicResource Surface.Card}"
                                CornerRadius="999"
                                BorderBrush="{DynamicResource Border.Default}"
                                BorderThickness="1">
                            <TextBlock Text="?" FontSize="10" FontWeight="Bold"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{DynamicResource Accent.PrimarySubtle}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource Accent.Primary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Button x:Name="HelpBtn" Style="{StaticResource HelpButtonStyle}"
                Click="HelpBtn_Click"/>
        <Popup x:Name="HelpPopup"
               PlacementTarget="{Binding ElementName=HelpBtn}"
               Placement="Bottom" StaysOpen="False" AllowsTransparency="True"
               PopupAnimation="Fade">
            <Border Background="{DynamicResource Surface.Elevated}"
                    BorderBrush="{DynamicResource Border.Default}" BorderThickness="1"
                    CornerRadius="8" Padding="14" MaxWidth="320" Margin="0,4,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="4"
                                      Color="Black" Opacity="0.4"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock x:Name="TitleBlock"
                               Style="{DynamicResource Text.H3}" Margin="0,0,0,6"/>
                    <TextBlock x:Name="BodyBlock" TextWrapping="Wrap"
                               Foreground="{DynamicResource Text.Secondary}"
                               FontSize="12" LineHeight="18"/>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>
